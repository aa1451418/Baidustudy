package main

import "fmt"

func main() {

	// m := map[int]string{1: "meike", 2: "yoyo", 3: "go"}

	var m1 map[int]string
	m1[1] = "name"
	fmt.Println(len(m1))
	// m2 := map[string]string
	// m2[2] = "mapmap"

	// m2[3] = "mapmap"
	// fmt.Println(len(m2))
}
